server:
  port: 18001

spring:
  application:
    name: eureka-client-one

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:18761/eureka/
